

Things to do before deploying:
1. "Ways startup website gets hacked"
2. Account based rate limiting
3. UI state rollback button ✅
4. Share UI button
5. Delete topic button ✅
6. User DTO add credit that tracks user credit, default 0, buy us coffee increase (?), read-only for frontend. Used to decide daily rate limiting (cuz our service is token intensive)
7. Migrate to supabase once every DTO is finalized